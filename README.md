# Image-Filtration-and-Noise-Reduction-with-CNN-
This project demonstrates the use of Convolutional Neural Networks (CNNs) to filter and reduce noise in images. It involves training a CNN model to process noisy images and produce clean, filtered outputs, showcasing the effectiveness of deep learning techniques in image preprocessing tasks.
